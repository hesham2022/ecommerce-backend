export enum ConversationKind {
  DIRECT = 'DIRECT',
  ORDER = 'ORDER',
}

export enum MessageAttachmentKind {
  IMAGE = 'image',
  FILE = 'file',
}

export enum ConversationReportStatus {
  OPEN = 'OPEN',
  RESOLVED = 'RESOLVED',
}
